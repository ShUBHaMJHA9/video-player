# MongoDB connection string (use your MongoDB Atlas or self-hosted URI)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority

# Database name
DB_NAME=bot

# Collection name
COLLECTION=videos

# Server port (Vercel ignores this, but useful for local dev)
PORT=3000
